CREATE TABLE users (
    id BIGINT NOT NULL IDENTITY GENERATED BY DEFAULT PRIMARY KEY,
    name VARCHAR,
    email VARCHAR,
    verified BOOLEAN NOT NULL DEFAULT FALSE,
    created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
);
